import React from "react";
import { ProfileDataPropsType } from "./ProfileInfo";

export const ProfileDataForm = (props: ProfileDataPropsType) => {
    return (
        <div>
            Form
        </div>
    )
}